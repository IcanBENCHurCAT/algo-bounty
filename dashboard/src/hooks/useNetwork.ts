'use client'
/**
 * useNetwork — thin wrapper around @txnlab/use-wallet-react's useNetwork hook.
 *
 * Exposes: activeNetwork, setActiveNetwork
 * Use this instead of hardcoding network IDs.
 */
export { useNetwork } from '@txnlab/use-wallet-react'
